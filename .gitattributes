# Canonical editable app file is `relentless-quest-in-development.html`.
# Legacy numbered snapshot files are intentionally excluded from source control
# so review tooling never has to process unsupported binary-style snapshot diffs.
# The canonical monolith is large; prefer current-branch content on merges to
# avoid unresolvable web-editor conflict blocks. Reapply desired changes in a
# follow-up commit when needed.
relentless-quest-in-development.html merge=ours
