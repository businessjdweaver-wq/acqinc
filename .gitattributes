# Canonical editable app file is `relentless-quest-in-development.html`.
# Keep this file as plain text with default merge behavior so repository
# merge requirements and protected-branch policies can run normally.
