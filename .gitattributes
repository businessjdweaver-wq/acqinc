# Canonical editable app file is `relentless-quest-in-development.html`.
# Legacy numbered snapshot files are intentionally excluded from source control
# so review tooling never has to process unsupported binary-style snapshot diffs.
# The canonical monolith is large; use union merges to reduce hard conflict
# blocks that fail in the web editor.
relentless-quest-in-development.html merge=union
